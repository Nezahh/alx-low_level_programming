#include <stdio>
**
*main - a program that print a line using printf function
Returns: 0 (success)
*/
int (void)
{   prinf (with proper grammer, but the out was a piece of art,n")
       Returns(0);
}	       
